using System.Collections.Generic;
using UnityEngine;

//플레이어 밑에 붙일 기믹들을 총 관리하는 매니저입니다
public class PlayerGimmickManager : MonoBehaviour
{
    //아마 기믹들의 이름을 넣을 List입니다 딕셔너리에서 key값으로 쓸겁니다
    //public enum GimmickList
    //{
    //    totem
    //}

    //이건 IGimmick 스크립트들을 모아놓은 리스트입니다 처음에 초기화할때 여기서 꺼내가서 딕셔너리에서 value로 쓸겁니다
    //[SerializeField] private 

    //private Dictionary<GimmickList, IGimmick> gimmickDict = new();

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
